-r requirements.txt

# Direct development dependencies only; pip resolves the transitive set.

# Testing
pytest==9.0.3
pytest-qt==4.5.0

# Lint + format (single tool: see [tool.ruff] in pyproject.toml)
ruff==0.15.18

# Type checking (non-blocking; see [tool.mypy] in pyproject.toml)
mypy==2.1.0

# Git hooks
pre-commit==4.6.0
